/* Vuex actions for module <%= upCaseName %> */

import { API, API_URL } from '@/config';

import {
  MUTATION_NAME
} from './mutation-types';

export default {
  // example
  exampleAction({ commit }) {
    commit(MUTATION_NAME)
  }
};